# Sparks - agent-top3pm

- [2026-02-23 15:24:54 PST] Post-discovery normalization reminder: after all entities are fully discovered/reviewed, normalize remaining snapshot-style fields into relation-first modeling where appropriate; current candidates are `ReleaseNote.author -> authoredBy Person` and `BranchRequest.DEV_LEAD/PM_LEAD -> direct Person relations`.
- [2026-02-24 20:27:55 PST] Use-case pattern reminder: after finalizing branch synchronization example in `preview/branch-sync-use-case-example-v0.1.md`, design parallel agentic use-case examples for other key entities/workflows (e.g., NFR intake sync, Issue resolution trace sync, Build/Artifact validation sync) using the same format (data model scope + full automation + pickup + skill policy).
- [2026-02-25 08:12:30 PST] Next-round modeling reminder: replicate the new `Agent Actions (First Round)` pattern (introduced on `NFR` in `v2.7`) across all entities, so each entity has explicit agent responsibilities for ingest/sync/derive/writeback policy.
